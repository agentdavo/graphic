# OMEGA — Through the Blue

A native `vkmin` + `sndmin` cinematic inspired by the supplied Omega-class
destroyer references. All ship geometry, surface detail, gate effects and sound
are generated by this demo. No model downloads or runtime shader compiler.

Double-click `OMEGA.cmd` in the repository root to run the UCRT build with sound.
It builds the executable if missing and sets the MSYS2 runtime DLL path.

| Control | Action |
| --- | --- |
| Space | Pause the sequence and mute playback |
| R | Restart the sequence |
| M | Mute / unmute |
| A / D | Orbit the camera |
| W / S | Raise / lower the camera |
| F12 | Save `omega-capture.png` in the working directory |
| Close window | Exit |

The 20-second loop follows the supplied gate-opening sequence:

| Time | Action |
| --- | --- |
| 0–1 seconds | Dormant pylons against black space |
| 0.25–2.2 seconds | A red-orange flare appears part way along each pylon, travels toward the far cap brightening at every fin station, and swells into a pink-white sphere |
| 1.9–2.9 seconds | The four spheres merge into a central white flash, with halo, streak and ring, that lights the structures |
| 2–4.5 seconds | A small blue ring with a dark centre expands into the funnel |
| 4.5–9.55 seconds | Tiny ship cruises rapidly through the distant corridor; a warm glow at the throat from 7 to 9.4 seconds announces its arrival |
| 9.55–13 seconds | Rapid perspective growth; the hull settles to 22 units/second and flies out past the camera |
| 10.25–16.25 seconds | Two short–short–long bursts, beginning halfway out of the gate |
| 13–17 seconds | The camera follows the receding hull, then pans back to the gate |
| 17.5–19.5 seconds | Funnel contracts and pylon power fades |
| 19.5–20 seconds | Structures remain in space before the sequence restarts |

Audio simulation advances on every 60 Hz tick, including ticks skipped
by slow rendering or isolated screenshot requests. Pausing mutes audio while
existing audio tails continue to advance silently.

The camera starts about 9 degrees off the gate axis and slightly above it,
86 units back, so the splayed pylons reach toward the viewer with the vortex
opening near their camera-side ends. From the first frame it drifts gently,
orbiting, dollying in to 80 units and rising, so the later moves continue
motion already under way. The pan begins at 6 seconds and finishes at 11
seconds, swinging to a three-quarter view 62 units out and 28 units beside the
corridor. From 11 to 12.6 seconds the look-at target slides from the gate to
the hull, which passes about 30 units from the camera; between 14.4 and 17.2
seconds it eases back to the gate for the closing. The shaders take the view
direction from the view-projection rows rather than a separate target.
Each cannon burst has two 0.23-second shots spaced 0.55 seconds apart, followed
by a 1.3-second sustained beam. Bursts repeat every 3.6 seconds, with a shared
tick schedule driving the beams, muzzle lighting and short/sustained sounds.

The ship has 11,184 triangles including three four-wing escorts, a rotating
counterbalanced habitat, bow cannon mantlets, antennae, ribs, windows, a reactor
block and four engine banks. Four fixed gate pylons bring the scene to 15,696
triangles. Each is an open box truss of four rust rails from z=38 inside the
cone to z=-4 toward the camera, splayed outward by 4 degrees about its far
cap, with a cross frame, a yellow window bay and a pair of long tangential
blade fins at each of seven stations, and an inward-facing emitter at the far
cap. The mouth sits about 12% of the way along them from the camera-side caps, so
most of each pylon reaches back into the gate's energy and shows through it
at reduced contrast, as in the reference footage. The ignition flash lights
their metal surfaces.

The rendering passes are a 2048-square directional shadow map, a separate HDR
gate layer, the composite HDR scene, quarter-resolution bloom, and a period
grade: highlights clip to white with a gentle knee, chroma bleeds sideways
while luma stays sharp, and fine per-tick grain is added. Hull
shading includes procedural armor panels, rivets, roughness, key lighting,
gate-colored rim lighting and transient red muzzle lighting. The
vortex is one analytic cone from the 14-unit mouth to a 1.5-unit throat 74
units in, so the opening reads as a cone with the dark throat offset toward
the vanishing point. The ship's approach route is far deeper than the cone;
inside the gate it is only ever seen through the mouth disc. Rays that miss the mouth disc see the funnel's outer skin
fading within a third of a radius, so from the off-axis camera the gate reads
as a feathered ellipse rather than a pipe. Big soft billows dominate the
energy, with finer advected strands only adding texture, and a faint
blue-violet nebula band sits behind the gate. Advected blue strands
fade into a nearly black cap at the distant throat, with a soft outer edge and
localized bloom against a black starfield. The ship remains visible
inside the tunnel. It starts at z=1180 and cruises at 220 units/second until
z=70 (9.55 seconds), then decelerates exponentially to a steady 22 units/second
and keeps going, crossing the mouth at 10.25 seconds, passing the camera at
about 12.8 seconds and reaching z=-213 by the end of the loop. Position and
speed are continuous at the transition.
Perspective makes the ship grow as it approaches; its geometry is not scaled.
Beyond the mouth the hull is clipped to the mouth disc, so it is seen only
through the opening: from the off-axis camera it first appears as a small
silhouette deep in the vortex at about 8.5 seconds and grows, and it is never
drawn beside the cone.
The mouth sits at z=1, about 12% of the way along the pylons. Its 14-unit radius fits
inside the pylons, whose axes sit 15.8 units from the corridor center at the
four cardinal positions before the splay.
A broad feather blends the edge into the stars, and outer wisps fade before
reaching beyond the containment structures. The cone's throat is at z=75; the ship's route begins at z=1180.
Plasma strands and rim wisps share a slow
0.10-radian/second rotation (about 5.7 degrees/second), with gentle internal flow.
The camera begins looking down the tunnel from off axis and moves to a wide
three-quarter view during emergence.

The original score, **Blue Circuit / Transit**, uses a 150 BPM pulse and a
75 BPM backbeat in F minor. Native sndmin patches provide warm detuned pads,
syncopated sub bass, a sixteenth-note arpeggio, kick, gated snare, toms and restrained hats. A dotted-eighth
delay adds space to the synths; drums stay dry. The beat enters during gate
expansion, the arpeggio intensifies during emergence, and a final chord fades
with the closing gate. All notes use the shared simulation clock.

The gate startup uses a brief lightning snap timed to the ignition flash,
followed by overlapping low-pass thunder fronts and a deep, falling sub-bass
roll. A shorter, softer thunder tail accompanies closure. There is no continuous
electrical crackle. These generated PCM effects, reactor hum and short/long
cannon reports are mixed by sndmin alongside the score. No external music
assets or recordings are required. See [audio reference notes](isn-audio-notes.md)
for measured features of the supplied MP4 and the limits of that analysis.

## MSYS2 builds

From PowerShell in the repository root:

```powershell
.\tools\build-msys2.ps1 -Toolchain ucrt64
.\tools\build-msys2.ps1 -Toolchain clang64
.\tools\run-omega.ps1 -Toolchain ucrt64
.\tools\run-omega.ps1 -Toolchain clang64
```

The default build target is `all`, including the existing tools, examples, audio
library and new scene. Add `-Target omega` to build just the demo. Each compiler
gets separate graphics and audio object directories. UCRT uses GCC `-fanalyzer`;
Clang uses its separate static analyzer for the new scene. Project warnings stay
enabled and remain errors on both compilers.

These scripts use `C:\msys64`, its `mingw32-make.exe`, its POSIX shell, and
`VULKAN_SDK` for headers, shader compilation and the Vulkan import library.
GLFW headers/libraries are not installed in the current MSYS2 prefixes; the script
uses the official [GLFW Windows package](https://www.glfw.org/download), pinned
to 3.5.1 under `build/deps/glfw`. It fetches that package only if missing.
The live audio backend uses the existing bundled miniaudio implementation.

```powershell
# Rebuild and capture a frame using a separate headless build.
.\tools\run-omega.ps1 -Toolchain ucrt64 -Headless -Rebuild -Frame 750

# Capture with the existing live binary (headless mode opens no audio device).
$env:PATH = "C:\msys64\ucrt64\bin;" + $env:PATH
.\build\omega-ucrt\ex_21_omega.exe --headless --frame 750 --out build/omega-ucrt/hero.png
.\build\omega-ucrt\ex_21_omega.exe --headless --frames 0,102,150,285,540,750,1110,1190 --out-dir build/omega-ucrt
.\build\omega-ucrt\ex_21_omega.exe --audio-only --audio-out build/omega-ucrt/blue-circuit-transit.wav
```

Per-frame state (view-projection, camera, time, ship position, aperture and
muzzle level) is one `OmegaScene` block in a ring allocation whose address
rides in a 32-byte push block with the pass selector and image indices; the
journal relocates that address on replay. A run of three frames or more
prints the GPU cost of each pass, for example:

```
omega: gpu ms shadow=0.62 gate=2.95 scene=1.19 bloom=0.90 grade=2.54 total=8.19
```

That is frame 238 at 1280x720 on the Intel GPU; the flyby at frame 688 costs
about 10 ms, so the 60 Hz budget holds with room to spare.

Use `OMEGA.cmd` for interactive playback. A binary built with `HEADLESS=1` has
no window backend; running it without arguments now prints that distinction
instead of attempting to open a window and aborting.

## Verification — 5 September 2026

- Complete native `all` builds passed with MSYS2 UCRT GCC 16.2.0 and Clang 22.1.8.
- Both live executables created a 1280×720 Vulkan swapchain and exited cleanly
  after bounded playback with the miniaudio backend.
- Frame 650 of the pulse revision was pixel-identical across compilers on the
  installed Intel GPU.
- Legacy and modern Vulkan paths produced identical frame-915 pixels in the
  preceding gate revision.
- All compiled shader modules passed `spirv-val`; runtime logs contained no
  Vulkan validation errors.
- Both compilers' `mat4_test`, `play_test`, `spirv_test` and `sndmin/test` passed.
- Clang static analysis and cppcheck for the scene passed.
- Both final audio renders were identical: stereo, 48 kHz, 20 seconds, peak 25,643 / 32,767,
  zero clipped samples.
- A Corridor frame loaded the existing Sponza and character assets and rendered
  successfully after the scene-loader alignment change.
- The gate ignition and pylon revision was rebuilt on both compilers; dormant,
  ignition, expansion, distant ship, approach, cannon-fire and collapse frames
  were inspected. The firing frame matched across compilers and Vulkan paths.
  A full live loop plus restart exited cleanly without validation messages.
- The pulse revision passed both compiler builds and Clang analysis. Captures
  confirmed two short flashes, dark gaps, and the sustained third beam. Audio
  stayed below clipping, and bounded live playback covered the first full burst.
- Emergence artifact fixes separate the bridge window from the bow armor,
  keep rotating hull texture projection in model space, and filter subpixel
  grain, panel seams and ribs. Both builds passed; corrected frame 720 matched
  across compilers and the hull shader passed SPIR-V validation.
- The forward-mouth and rotation revision passed both builds, with matching
  frame-710 pixels and valid gate/hull SPIR-V. Opening, emergence and closing
  frames were inspected.
- The final 20-second score and soft-lip revision passed both compiler builds
  and Clang analysis. Frame 750 matched across compilers, both changed shaders
  passed SPIR-V validation, and the pylons no longer intersect an opaque gate
  wall. Opening, firing and closure frames were inspected.
  Full live playback through the 20-second loop and restart completed cleanly.
  The subsequent softer-crackle/containment adjustment passed both builds,
  with identical audio and frame-750 output across compilers.
- The deep-transit revision uses longitudinal pylons and late braking, guided
  by the supplied stills and the jump sequence around 39–40 seconds of `isn.mp4`.
  Both builds passed; frame 615 and the full audio mix matched across compilers.
  Distant approach, rapid emergence, firing and closure frames were inspected.
- The selective pylon veil and Transit music revision passed both compiler
  builds and shader validation. Frame 300 and full audio output matched across
  compilers; opening, active masking, ship emergence and closure were inspected.
- The first ten seconds of `b5.mp4` were inspected at half-second intervals,
  with a detailed frame at 5.5 seconds. This informed finer warped plasma
  strands and a later, softer pylon fade that preserves more forward structure.
  Both builds passed; frame 300 remained identical across compilers, and all
  Omega shader modules passed SPIR-V validation.
- Closure now contracts about the fixed distant throat, with perspective
  compensation for a smooth visible shrink and a stationary automatic camera.
  The pylon veil follows the same contracting cone. Both compiler builds passed;
  closure frame 1110 and the revised thunder audio matched across compilers.
  Closing frames were inspected and gate/hull SPIR-V validation passed. The
  20-second mix has no clipped samples (peak 25,643 of 32,767).

This is targeted build and runtime verification; the entire historical golden
image suite was not rerun. Cross-device bit equality and sustained performance
on other GPUs are not asserted.

Clang compatibility fixes also separate `cgltf`'s implementation from the cooker,
keep dependency headers out of compiler link inputs, add checked scene-record
alignment and printf annotations, and use a double-precision infinity in the
valley's timing accumulator.

Source: `examples/21_omega.c`, `demo/omega_score.h`, `demo/omega_shared.h`, and `shaders/omega*`.

/* Keep generated data in its own translation unit so editing animation does
 * not feed half a million constant vertices through the optimizer again. */
#define OMEGA_MODEL_IMPLEMENTATION
#include "omega_model.h"
